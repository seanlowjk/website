export type JobExperienceItem = {
  title: string;
  role: string;
  duration: string;
  description: string;
};

export type ProjectItem = {
  title: string;
  summary: string;
  backgroundImageUrl: string;
  githubLink: string;
};
